#!/bin/bash
set -eo pipefail

script_dir=$(cd -- "$(dirname -- "${0}")" && pwd)

# dnf
sudo dnf -y upgrade
sudo dnf -y install systemd systemd-networkd systemd-container

# bridge
"${script_dir}/install_bridge"

# image
"${script_dir}/build_image"
